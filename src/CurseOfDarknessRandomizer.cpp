// CurseOfDarknessRandomizer
#include <iostream>
#include <fstream>
#include <iomanip>
#include "Program.h"

int main(int argc, char** argv)
{
	Program program(argc, argv);

	//std::fstream debug;
	//debug.open("HexTest.bin", std::ios::out | std::ios::in | std::ios::binary);
	//
	//uint32_t add = 0x00000024;
	//
	//uint32_t val = 10;
	//
	//debug.seekp(add);
	//debug.write(reinterpret_cast<char *> (&val), 1);
}